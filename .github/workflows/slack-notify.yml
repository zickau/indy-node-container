name: slack-notify
on:
  workflow_dispatch:
  
jobs:
  notify-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run a one-line script
        run: echo Hello, world!
